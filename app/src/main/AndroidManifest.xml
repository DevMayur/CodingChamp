<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.incarta.clearner">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProgrammingChamp"
        tools:ignore="AllowBackup"
        android:hardwareAccelerated="true"
        tools:targetApi="n">
        <activity android:name="com.incarta.clearner.activities.CustomCodeViewerActivity"/>
        <activity
            android:name="com.incarta.clearner.activities.ContentActivity"
            android:label="@string/title_activity_content"
            android:theme="@style/Theme.ProgrammingChamp.NoActionBar" />
        <activity android:name="com.incarta.clearner.activities.HomeActivity" />
        <activity android:name="com.incarta.clearner.activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id"/>

    </application>



</manifest>